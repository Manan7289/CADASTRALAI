Flask==3.1.2
numpy==2.3.4
opencv-python-headless==5.0.0.93
pillow==12.0.0
requests==2.32.5
scikit-learn==1.7.2
scipy==1.16.3
shapely==2.1.2

# backend/ml/ only -- the OSM + RandomForest pipeline above runs without these.
# Install the CUDA build explicitly if you have an NVIDIA GPU, since plain
# `pip install torch` gives you the CPU wheel and training then takes hours:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
segmentation-models-pytorch==0.5.0
huggingface-hub==1.31.0
